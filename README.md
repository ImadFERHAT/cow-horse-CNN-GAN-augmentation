# cow-horse-CNN-GAN-augmentation
A convolutional neural network (CNN) for classifying images of cows and horses, enhanced with dataset augmentation using GAN, cGAN, and CycleGAN models to improve accuracy.
